services:
  api:
    build:
      context: ./backend      
      dockerfile: Dockerfile
    container_name: agrogear-api
    restart: always
    networks:
      - shared-proxy
    environment:
      - DB_HOST=${DB_HOST}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - PORT=${PORT}
      - JWT_SECRET=${JWT_SECRET}
      - API_HOST=${API_HOST}
      - PREFIX=${PREFIX}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.agrogear.rule=Host(`${API_HOST}`) && PathPrefix(`/${PREFIX}`)"
      - "traefik.http.routers.agrogear.entrypoints=websecure"
      - "traefik.http.routers.agrogear.tls.certresolver=myresolver"
      - "traefik.http.services.agrogear.loadbalancer.server.port=9876"

      - "traefik.http.middlewares.strip-agrogear.stripprefix.prefixes=/agrogear"
      - "traefik.http.routers.agrogear.middlewares=strip-agrogear"

networks:
  shared-proxy:
    external: true